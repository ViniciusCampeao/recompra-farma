name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Pull latest and deploy
        run: |
          cd ~/recompra-farma
          git fetch origin main
          git reset --hard origin/main
          docker compose up -d --build
